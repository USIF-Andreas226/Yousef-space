{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd portfolio/chatbot && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd portfolio/chatbot && npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
